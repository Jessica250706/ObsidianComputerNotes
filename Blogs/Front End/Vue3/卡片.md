---
tags:
  - 前端
---
```html
<el-card
  v-for="item in virtualMachinesLists"
  :key="item.vmUuid"
  class="!shadow-[4px_4px_10px_0px_rgba(0,0,0,0.2), -2px_-2px_10px_0px_rgba(0,0,0,0.15)] h-[182px] rounded-[10px]"
  shadow="always"
  :body-style="{
	padding: '0px',
	margin: '0px',
	height: '100%',
	width: '100%',
  }"
>
  <!-- 上半：资源信息 -->
  <div class="flex h-[108px] flex-col bg-[#F6F6FF] px-[16px] text-[14px] leading-[22px] ">
	<!-- 第一行：资源占用+状态+更多+单选 -->
	<div class="mb-[9px] mt-[11px] flex flex-row items-center justify-between">
	  <!-- 左：资源占用+状态 -->
	  <div class="flex flex-row items-center justify-start">
		<span class="line-height-[22px] mr-[10px] font-bold text-[#303133]">资源占用</span>
		<div
		  :class="{
			'border bg-[#E8F0FE] text-[#1E5EFF] shadow-[inset_0_0_0_1px_#1E5EFF]': item.state ? (StateToChinese[item.state] === '创建中' || StateToChinese[item.state] === '运行中' || StateToChinese[item.state] === '创建中' || StateToChinese[item.state] === '创建成功' || StateToChinese[item.state] === '创建失败' || StateToChinese[item.state] === '启动中') : '---',
			'border bg-[#FFF6E6] text-[#C26C03] shadow-[inset_0_0_0_1px_#C26C03]': item.state ? (StateToChinese[item.state] === '重置中' || StateToChinese[item.state] === '重置成功' || StateToChinese[item.state] === '重置失败' || StateToChinese[item.state] === '重启中') : '---',
			'border bg-[#F4F4F5] text-[#303133] shadow-[inset_0_0_0_1px_#303133]': item.state ? (StateToChinese[item.state] === '关闭中' || StateToChinese[item.state] === '已关闭') : '---',
		  }"
		  class="inline-block h-[24px] w-[66px] rounded-[60px] border text-[14px]"
>
		  <span class="flex h-[24px] w-full items-center justify-center">{{ item.state ? StateToChinese[item.state] : '未知状态' }}</span>
		</div>
	  </div>
	  <!-- 右：更多+单选 -->
	  <div class="flex w-[56px] flex-row items-center justify-end">
		<!-- 更多 -->
		<more-button
		  class="shrink-0"
		  :fatherMessage="item"
		/>
		<!-- 单选框 -->
		<label class="relative ml-[10px] inline-flex cursor-pointer items-center">
		  <input
			:checked="checkedVMGroupId.includes(item.vmUuid)"
			:indeterminate="false"
			type="checkbox"
			class="relative size-[14px] appearance-none rounded-[2px] border border-[#DDE0E6] bg-[#FFFFFF] checked:border-[#1E5EFF] checked:bg-[#1E5EFF] checked:after:absolute checked:after:left-1/2 checked:after:top-1/2 checked:after:-translate-x-1/2 checked:after:-translate-y-1/2 checked:after:text-xs checked:after:font-bold checked:after:text-[#FFFFFF] checked:after:content-['✓']"
			@change="toggleVirtualMachineCheck(item.vmUuid)"
		  />
		</label>
	  </div>
	</div>
	<!-- 资源占用数据：CPU+GPU+内存+磁盘 -->
	<div class="grid grid-cols-2 grid-rows-2 gap-x-[8px] gap-y-[10px] text-left">
	  <div class="truncate">
		<span>CPU：{{ item.hardwareSpec?.cpu !== null && item.hardwareSpec?.cpu !== undefined ? formatSize(item.hardwareSpec?.cpu, 'cpu') : '-' }}核（{{ item.resource?.cpu ?? '-' }}%）</span>
	  </div>
	  <div class="truncate">
		<span>GPU：{{ item.hardwareSpec?.gpu !== null && item.hardwareSpec?.gpu !== undefined ? formatSize(item.hardwareSpec?.gpu, 'gpu') : '-' }}卡</span>
	  </div>
	  <div class="truncate">
		<span>内存：{{ item.hardwareSpec?.mem !== null && item.hardwareSpec?.mem !== undefined ? formatSize(item.hardwareSpec?.mem, 'memory') : '-' }}GB（{{ item.resource?.mem ?? '-' }}%）</span>
	  </div>
	  <div class="truncate">
		<span>磁盘：{{ item.hardwareSpec?.disk !== null && item.hardwareSpec?.disk !== undefined ? formatSize(item.hardwareSpec?.disk, 'disk') : '-' }}GB（{{ item.resource?.disk ?? '-' }}%）</span>
	  </div>
	</div>
  </div>
  <!-- 下半：实验信息 -->
  <div class="grid h-[74px] w-full grid-cols-2 grid-rows-2 gap-x-[8px] gap-y-[10px] bg-[#FFFFFF] px-[16px] py-[10px] text-left">
	<div class="flex items-center">
	  <span class="flex size-full items-center truncate text-left">实验人：{{ truncatedExperimentUser(item.user || '-') }}</span>
	</div>
	<div class="flex items-center">
	  <span class="flex size-full items-center truncate text-left">时长：{{ formatDuration(item) }}</span>
	</div>
	<div class="col-span-2 flex items-center">
	  <span class="truncate text-left">实验名称：{{ item.experimentName || '-' }}</span>
	</div>
  </div>
</el-card>
```