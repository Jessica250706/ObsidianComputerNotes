# 定义

```C++
#include <stack>
```

# 初始化

  

# 元素访问

  

# 常用函数

|函数说明|接口说明|
|---|---|
|stack()|构造空的栈|
|empty()|检测stack是否为空|
|size()|返回stack中元素的个数|
|top()|返回栈顶元素的引用|
|push()|将元素val压入stack中|
|pop()|将stack中尾部的元素弹出|

