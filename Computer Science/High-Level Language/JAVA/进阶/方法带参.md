# 方法带参

## 构造方法带参

### 语法

```Java
访问修饰符 类名(数据类型1 变量名1,数据类型2 变量名2,...数据类型n 变量名n){
    
}
```

### 举例

```Java
public class Person{
    // 变量
    public String name; // 姓名
    public int age; // 年龄

    // 方法：访问修饰符  返回值类型  方法名([参数列表])
    public void sleep(){
        System.out.println("sleep");
    }
    public void eat(){
        System.out.println("eat");
    }

    // 构造方法
    public Person(String name, int age){
        this.name = name;
        this.age = age;
    }
}
```

```Java
public final class Main {
    public static void main(String[] args) {
        Person p = new Person("Jessica", 21);
    }
}
```

## 方法带参

### 语法

```Java
访问修饰符 返回值类型 方法名(数据类型1 变量名1,数据类型2 变量名2,...数据类型n 变量名n){
	// return关键字的作用就是给出方法执行的结果，使得方法直接结束
	[return 返回值;]
}
// 带参方法调用
对象名.方法名(实参1,实参2,...实参n);
```

## 对象数组

### 举例

```Java
public class Student {
    // 变量
    public String name; // 姓名
    public int age; // 年龄

    // 构造方法
    public Student(String name, int age){
        this.name = name;
        this.age = age;
    }
}
```

```Java
public class StudentTest {
    public static void main(String[] args) {
        Student[] students = new Student[2];
        students[0] = new Student("Phainon", 28);
        students[1] = new Student("Hyacine", 122);
    }
}
```

![image 16.png](images/image%2016.png)

## 引用数据类型作为方法的参数

### 举例

```Java
public class Student {
    // 变量
    public String name; // 姓名
    public int age; // 年龄

    // 构造方法
    public Student(String name, int age){
        this.name = name;
        this.age = age;
    }
}
```

```Java
public class StudentTest {
    public Student[] students = new Student[10];
    
    // 引用数据类型作为方法的参数
    public void testStudent(Student student){
        for (int i = 0; i < students.length; i++){
            if (students[i] == null){
                students[i] = student;
            }
        }
    }
}
```

## 方法传参传递规则

1. Primitive arguments, such as an int or a double, are passed into methods by  
    value. This means that any changes to the values of the parameters exist only  
    within the scope of the method. When the method returns, the parameters are gone and any changes to them are lost.  
    
    基本数据类型的参数（例如int或double）按值传递给方法。 这意味着对参数值的任何更改仅存在于方法范围内。 当方法返回时，参数消失，对它们的任何更改都将丢失。
    
2. Reference data type parameters, such as objects, are also passed into methods by value. This means that when the method returns, the passed-in reference still references the same object as before. However, the values of the object's fields can be changed in the method, if they have the proper access level.
    
    引用数据类型参数（例如对象）也按值传递到方法中。 这意味着当方法返回时，传入的引用仍然引用与以前相同的对象。 但是，如果对象的字段的值具有适当的访问级别，则可以在方法中更改它们。
    

### 基本数据类型：拷贝传参

```Java
public final class Main {
    
    public static void change(int number) {
        number += 1;
    }
    
    public static void main(String[] args) {
        int a = 10;
        change(a); // 调用方法时，实际上传递的是变量a的值的拷贝
        System.out.println(a);
    }
    
}
```

### 引用数据类型：实际参数

传递的是堆内存的空间地址域。

```Java
public final class Main {

    public static void change(Student student) {
        student.age = 20;
    }

    public static void main(String[] args) {
        Student s = new Student("Jessica", 22);
        change(s); // 此处传递的参数就是实际参数
        System.out.println(s.age);
    }
}
```

![image 1 4.png](images/image%201%204.png)

# Overloading/方法重载

## 概念

在**同一个类**中，**方法名相同**，**参数列表不同**的多个方法构成方法重载

## 举例

```Java
public class Calculator {

    public int sum(int a, int b){
        return a + b;
    }
    
    public int sum(int a, int b, int c){
        return a + b + c;
    }
}
```

## 构造方法重载

构造方法也是方法，因此构造方法也可以重载。如果在一个类中出现了多个构造方法的定义，那么这些构造方法就形成构造方法重载。

```Java
public class Student {
    // 变量
    public String name; // 姓名
    public int age; // 年龄

    // 构造方法
    public Student(String name, int age){
        this.name = name;
        this.age = age;
    }
    
    public Student(){
        
    }
}
```

p.s. this关键字调用构造方法，必须是这个构造方法中的第一条语句。

# 面向对象和面向过程的区别

## 对比

面向过程侧重点在过程的实现上。面向对象的侧重点在对象上，需要利用对象的行为来完成过程的组装。