# 设计数据库

## WHAT-实体

**实体**就是软件开发过程中所涉及到的事物，通常都是**一类数据对象的个体**。

## WHAT-数据库设计

数据库设计就是将实体与实体之间的关系进行规划和结构化的过程。

## WHY-对数据库进行设计

当存储的数据比较少的时候，当然不需要对数据库进行设计。但是，当对数据的需求量越来越大时，对数据库的设计就很有必要性了！如果数据库的设计不当，会造成数据冗余、修改复杂、操作数据异常等问题。而好的数据库设计，则可以减少不必要的数据冗余，通过合理的数据规划提高系统的性能。

## HOW-设计数据库

### 收集信息

在确定客户要做什么之后，收集一切相关的信息，尽量不遗漏任何信息。

### 标识实体

实体一般是名词，每个实体只描述一件事情，不能重复出现含义相同的实体。

### 标识实体的详细属性

标识每个实体需要存储的详细信息。

### 标识实体之间的关系

理清实体与实体之间的关系。

# ER图

## WHAT

ER = Entity Relational （实体关系）

ER图就是实体关系图

## HOW-绘制ER图

![image 34.png](images/image%2034.png)

### 举例

![image 1 13.png](images/image%201%2013.png)

# 数据库模型图

## WHAT-关系模式

实体关系的描述称为**关系模式**，关系模式通常使用**二维表**的形式表示。

### 举例

学生（学号，姓名，性别，年龄，所属班级）

班级（班级编号， 班级名称）

## 关系模式转为数据库模型图

将关系模式使用Navicat工具转换为数据库模型图，转换步骤如下：

- 将各实体转换为对应的表，将各属性转换为各表对应的列
- 标识每个表的主键列
- 在表之间建立主外键，体现实体

### 举例

![image 2 12.png](images/image%202%2012.png)

# 数据库三大范式

## 第一范式

第一范式是最基本的范式，确保每列保持**原子性**，也就是每列不可再分。

### 举例

![image 3 9.png](images/image%203%209.png)

## 第二范式

第二范式是在第一范式的基础上，每张表的属性**完全依赖于主键**，也就是每张表只描述一件事情。

### 举例

![image 4 7.png](images/image%204%207.png)

## 第三范式

第三范式是在第二范式的基础上，确保每列都**直接依赖**于主键，而不是间接依赖于主键，也就是**不能存在传递依赖**。比如A依赖于B，B依赖于C，这样A间接依赖于C。

### 举例

![image 5 7.png](images/image%205%207.png)